- defaults:
            link:
                          relink: true

- clean: ['~']

- link:
            ~/.vimrc: vimrc
            ~/.zshrc: zshrc
            ~/.vim: vim

- shell:
          - [git submodule update --init --recursive, Installing submodules]
